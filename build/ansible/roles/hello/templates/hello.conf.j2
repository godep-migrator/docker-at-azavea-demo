description "Hello"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 50 5

kill timeout 5

setuid {{ hello_user }}

env MARTINI_ENV=production

exec {{ hello_dir }}/bin/hello >> {{ hello_log }} 2>&1
